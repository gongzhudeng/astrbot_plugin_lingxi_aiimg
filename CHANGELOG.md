# 更新日志

## [v1.3.0] - 2026-09-01

### 新增

- 自拍前缀支持 `{today_outfit}` 与 `{lighting}` 占位符；今日穿搭从 BusySchedule 共享上下文读取并自动剔除内衣、内裤。
- 自拍穿搭支持可配置自动检测关键词，以及“沿用/不使用今日穿搭”控制词；临时服装描述优先于今日穿搭，局部拍摄提示词保持不变。
- 新增可配置时间光线规则，默认细分为黎明、白天、黄昏、夜间四段；旧版自拍前缀中的智能时间推断规则改由 `{lighting}` 占位符统一控制。

## [v1.2.8] - 2026-09-01

### 优化

- 为 `skills_like` 模式分层统一 `aiimg_generate` 工具说明，保留现有 `mode` 接口兼容性。
- 首轮仅说明生图与改图用途；自拍、文生图、编辑、批量及合规规则移入对应参数 schema。

## [v1.2.7] - 2026-08-16

### 变更

- 收紧 `aiimg_generate` 工具说明：文生图模式不得自行使用，只有用户明确要求时才能使用。
- 明确编辑模式仅能在用户提供或引用图片并要求修改时使用，不得自行调用。
- 扩展自拍模式定义：第一人称拍摄食物、物品、环境等不露脸画面仍属于自拍模式，不应切换为文生图。
- 补充提示词合规要求：提示词不得过度暴露或违规，否则会审核失败。

## [v1.2.4] - 2026-08-08

### 修复

- `aiimg_generate` 省略 `mode` 或传入 `auto` 时统一使用 `selfie_ref`；仅显式 `text` 或 `edit` 分别进入文生图和改图，不再按图片或提示词静默改路由。
- 自拍参考照缺失时明确失败，不再回退为文生图；显式 `text` 继续受 `features.draw.llm_tool_enabled` 控制。
- 成功发送和后台受理的工具结果不再向模型回传路径、媒体标识、任务 ID、JSON 状态或提示词；失败仍返回明确结果。
- 会话历史使用受边界保护的 `<image_history_record>`，仅保存 AI 主体描述和真实发送事实，不保存预设、拼接提示词、路径或内部状态。

### 测试

- 覆盖默认/`auto` 自拍路由、显式文生图开关、显式改图、后台受理脱敏和历史记录边界。

## [v1.2.3] - 2026-08-08

### 修复

- `aiimg_generate` 的工具说明明确限定为默认单图；仅当用户明确提出批量要求时，LLM 才可设置 `count > 1`。

## [v1.2.2] - 2026-08-08

### 修复

- 图片实际发送成功后，才向会话写入一条幂等的 `assistant` 历史；失败、未确认或重复完成事件不会写入成功事实。
- 会话历史仅保留原始用户提示词、规范化模式和生成数量，不再记录配置前缀、`effective_prompt`、内部规则、参考图来源或媒体标识。
- 后台生图完成通知改为临时用户内容注入，不再修改稳定的系统提示词；终态请求保持原有工具集合。
- 覆盖同步、批量、后台、改图、自拍和预设等成功路径，统一历史记录语义。

### 测试

- 补充历史内容隔离、幂等写入、后台临时注入和稳定工具集合的回归覆盖。

## [v1.2.0] - 2026-03-14

### 新增

- 统一图片 LLM 工具 `aiimg_generate` 增加 `count` 参数：`count=1` 执行单图，`count>1` 复用内部批量规划和执行流程。
- `aiimg_generate` 默认使用持久化后台任务：单图在 provider 完成前返回，批量在 planner 完成前返回，等待期间可继续正常聊天。
- 后台图片按计划发送后会重入原会话消息管线生成一次自然终态回应；同一会话的多个终态通知会等待发送确认并串行处理。
- 新增全局 `features.background_llm_image` 配置，提供启用开关、共享 provider 并发和按图片张数计算的后台容量。
- 保留手动 `/批量n ...` 命令，不再向 LLM 暴露独立批量图片工具。

### 成本保护

- 所有相关图片 provider 子请求在合并 `extra_body` 后强制固定 `n=1`，防止自定义参数导致服务商额外生成未被插件消费的图片。
- 批量严格按 `count` 创建独立单图任务；批量并发配置只限制同时执行数，不增加任务数量。
- 批量并发设为 `1` 时按顺序串行生成，不会禁用批量。

### 配置与兼容

- 后台生图默认开启，仅用于 `aiimg_generate`；手动 `/批量n`、`/自拍`、`/改图` 等命令继续使用同步链路。
- 后台链路仅支持 `aiocqhttp` / `weixin_oc`；平台能力不足、开启 streaming response、无法取得稳定 conversation 或账本不可用时自动同步降级。
- AstrBot 快速重启遇到旧实例租约时先同步降级，租约过期后自动接管后台账本，无需再次重启插件。
- `/stop` 可取消当前会话后台图片任务；成功的 `/reset` / `/new` 会取消并隔离旧会话任务。
- 图片发送继续复用原发送与 BusySchedule 媒体记录链路；发送状态无法确认时不会盲目重发。
- 单图调用保持向后兼容，旧调用未传 `count` 时默认生成一张。
- 数量非法或超过 `features.batch.max_count` 时直接拒绝，不静默修改请求数量。

## [v4.3.5] - 2026-06-09

### 修复

- 自拍失败时不再发送任何聊天文本提示, 仅保留原有表情反馈与日志记录。

## [v4.3.4] - 2026-06-08

### 修复

- 修复 AstrBot 4.25.x 下 `/自拍` 可能因 wake prefix 被框架剥离后未进入兜底 handler, 导致无响应的问题。
- 修复 `图片 + /自拍参考 设置` 这类图片在前、命令在后的消息可能不触发参考照设置的问题。

### 测试

- 补充自拍命令兜底测试, 覆盖裸 wake 命令、带前缀命令、已激活 command handler 去重、未唤醒裸文本忽略以及图片前置 `/自拍参考 设置`。

## [v4.3.3] - 2026-05-29

### 修复

- 修复 QQ / OneBot 场景下, 改图预设通过 @ 他人头像作为参考图时可能失效的问题。
- 兼容标准 `At` 消息段, OneBot raw `at` 段和 `[CQ:at,qq=...]` 原始消息。
- 兼容 `@用户 /预设` 这类命令不在首个文本段的触发顺序。

### 测试

- 补充 @ 头像参考图解析测试, 覆盖 raw OneBot at 段, raw CQ at 字符串, 以及忽略 @自己 / @全体。

## [v4.3.2] - 2026-05-25

### 稳定性

- 修复个人微信 `weixin_oc` 发送前优化副本长期残留的问题。
- 优化后的 `weixin_send_*.jpg` 临时副本会在发送流程结束后自动清理。
- 生成新副本前会按数量与时间清理历史残留，避免高频生成图场景下 `Temp` 目录持续堆积。
- 优化副本文件名加入唯一后缀，避免同一源图并发发送时复用同一个临时文件。

### 测试

- 补充 `weixin_oc` 发送临时副本清理与残留清理测试。

## [v4.3.1] - 2026-05-24

### 新增

- 新增个人微信 `weixin_oc` 图片发送前优化配置：
  - `send.weixin_compress_images`
  - `send.weixin_image_max_side`
  - `send.weixin_image_max_size_kb`
  - `send.weixin_api_timeout_seconds`
- 发送图片前会识别当前事件平台；仅当平台为 `weixin_oc` 时，才生成高质量 JPEG 发送副本并调整适配器 API/CDN 上传超时。

### 稳定性

- 降低个人微信发送 4K / 大体积生成图时触发 `upload_to_cdn TimeoutError` 的概率。
- QQ / OneBot 原有图片发送、文件兜底、compact bytes 兜底逻辑保持不变。

### 文档与元数据

- README 补充个人微信发送前处理配置、平台限制和超时排障说明。
- `metadata.yaml` 增加 `weixin_oc` 支持平台提示。

## [v4.3.0] - 2026-04-26

### 新增

- 新增文生图预设能力，支持通过 `features.draw.presets` 配置预设，并使用 `/文生图 预设名 补充提示词` 调用。
- 新增统一批量命令入口，支持：
  - `/批量n aiimg ...`
  - `/批量n 文生图 ...`
  - `/批量n aiedit ...`
  - `/批量n 自拍 ...`
  - `/批量n 改图预设名 ...`
- 新增 `LLM` 批量工具 `aiimg_batch_generate`，支持先规划多条不重复提示词，再一次性批量生成整组图片。
- 批量结果统一改为单张直接发送。
- 批量发送不再额外附带“标题 / 提示词 / 状态 / 失败提示”这类通知文本，只保留原插件自己的表情反馈。

### 配置增强

- 新增 `features.draw.batch_concurrency`，单独控制文生图批量并发。
- 新增 `features.edit.batch_concurrency`，单独控制改图 / 自拍批量并发。
- 新增 `features.batch.max_count`，控制单次批量最大数量，默认 `8`。
- 新增 provider 级 `generate_request_mode` / `edit_request_mode`，支持 `auto`、`stream`、`non_stream`。

### LLM 工具与批量规划

- `aiimg_generate` 继续支持 `auto` / `text` / `edit` / `selfie_ref` 路由。
- `aiimg_batch_generate` 默认批量数量为 `4`，建议范围 `2-8`，最终会被 `features.batch.max_count` 限制。
- 批量规划器会要求 `LLM` 输出 `title`、`prompt`、`variation_focus`，并对数量与去重结果做校验。
- 批量规划更适合同场景、同穿搭、不同姿势 / 角度 / 表情的成组出图需求。

### 兼容性与稳定性

- 修复 `ProviderRegistry` 中新默认 `auto` 覆盖旧 `enable_stream_*` 布尔配置的问题。
- 对齐 `ProviderRegistry` 与 `OpenAIChatImageBackend` 的 `request_mode` 兼容语义：
  - 显式 `stream` / `non_stream` 优先
  - `auto + enable_stream_*` 保留旧配置行为
  - 都没有时才回到默认 `auto`
- 统一 `validate()` warning 文案与运行时实际回退行为，避免“文档一套、执行一套”。
- 对单路径 provider 增加 `request_mode` 忽略提示，减少误解。

### 文档与元数据

- 重写 `README.md`，补充新功能的实际调用方式、批量命令、`LLM tool`、并发配置、平台限制与请求模式说明。
- 为插件补充 `metadata.yaml` 中的 `astrbot_version` 与 `support_platforms` 提示。
- 新增本 `CHANGELOG.md`，开始记录版本更新内容。

## [v4.2.26] - 历史基线版本

- 这是补充 `CHANGELOG` 之前的基线版本号。
- 更早的历史更新尚未回填到本文件。
